# Default values for mychart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
namespace: kube-system-monitor

aggregationImage:
  repository: grafana/grafana
  tag: 6.0.0
  pullPolicy: IfNotPresent
